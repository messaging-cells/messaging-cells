
/*------------------------------------------------------------

binder.cpp  

binder class.

--------------------------------------------------------------*/

#include "binder.hh"
#include "global.h"

/*
binder* // static 
bj_epiphany_binder_get_right(binder* bdr, bj_core_id_t id){
	BJK_CK(ck_id_1, (id == bj_addr_get_id(bdr)));
	binder* loc_rgt = bjk_pt_to_binderpt(bdr->bn_right);
	binder* glb_rgt = (binder*)bj_addr_set_id(id, loc_rgt);
	return glb_rgt;
}

binder* // static 
bj_epiphany_binder_get_left(binder* bdr, bj_core_id_t id){
	BJK_CK(ck_id_2, (id == bj_addr_get_id(bdr)));
	binder* loc_lft = bjk_pt_to_binderpt(bdr->bn_left);
	binder* glb_lft = (binder*)bj_addr_set_id(id, loc_lft);
	return glb_lft;
}

*/
